# Server (packages/server). Used when running server locally.
# When using Docker Compose, DATABASE_URL is set by the compose file for the server container.
# Copy to .env (repo root or packages/server) and adjust as needed.

DATABASE_URL=postgresql://meeting_prep:meeting_prep@localhost:5432/meeting_prep
PORT=3000

# LLM (required for POST /api/generate)
OPENAI_API_KEY=
# Optional; defaults to gpt-4o-mini
LLM_MODEL=

# CORS: comma-separated allowed origins (e.g. http://localhost:5173, https://your-app.vercel.app)
# Optional; default includes http://localhost:5173
ALLOWED_ORIGINS=
